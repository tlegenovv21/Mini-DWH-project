version: 2

models:
  - name: stg_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: email
        tests:
          - not_null

  - name: stg_products
    columns:
      - name: product_id
        tests:
          - unique
          - not_null

  - name: stg_orders
    columns:
      - name: order_id
        tests:
          - not_null
          # unique test might fail if raw has duplicates unless we dedup in staging SQL. 
          # Assuming proper loading or adding simple dedup logic like `DISTINCT` or `argMax` if needed.
          # For now, trusting raw is clean-ish or tests will catch.
          - unique 
      - name: status
        tests:
          - accepted_values:
              values: ['new', 'paid', 'cancelled']

  - name: stg_order_items
    columns:
      - name: order_id
        tests:
          - not_null
          - relationships:
              to: ref('stg_orders')
              field: order_id
